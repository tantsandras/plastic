import styled from "styled-components"

export const Button = styled.button`
  border: none;
  background: none;
  margin: 60px;
  font-weight: bolder;
  &:hover {
    /* background: #555; */
    font-size: 3rem;
  }
`
